# Sample environment variables for local development
# Copy this file to .env and adjust values as needed.

# Required: PostgreSQL configuration (used by docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=n8n
POSTGRES_NON_ROOT_USER=n8n
POSTGRES_NON_ROOT_PASSWORD=n8n

# Optional: n8n runtime configuration (docker-compose already sets defaults)
# N8N_RUNNERS_ENABLED=true
# N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
# N8N_BLOCK_ENV_ACCESS_IN_NODE=false

# Optional: timezone
# TZ=UTC
